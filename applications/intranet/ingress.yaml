apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: intranet-frontend-ingress
  namespace: intranet
  annotations:
    kubernetes.io/ingress.class: traefik
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    traefik.ingress.kubernetes.io/redirect-to-https: "true"
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.tls.certresolver: letsencrypt # 추가된 부분
spec:
  rules:
  - host: intranet.prod.kro.kr
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: intranet-frontend
            port:
              number: 80
  tls:
  - hosts:
    - intranet.prod.kro.kr
    secretName: intranet-frontend-tls
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: intranet-backend-ingress
  namespace: intranet
  annotations:
    kubernetes.io/ingress.class: traefik
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    traefik.ingress.kubernetes.io/redirect-to-https: "true"
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.tls.certresolver: letsencrypt # 추가된 부분
spec:
  rules:
  - host: intranet.back-end.kro.kr
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: intranet-backend
            port:
              number: 4000
  tls:
  - hosts:
    - intranet.back-end.kro.kr
    secretName: intranet-backend-tls